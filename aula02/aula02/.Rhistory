7*2
dados <- rnorm(10000:20000,
mean = 62,
sd = 7)
dados
dados <- rnorm(15000,
mean = 62,
sd = 7)
dados
dados <- rnorm(11500,
mean = 62,
sd = 7)
dados
dados <- rnorm(11500,
mean = 62,
sd = 7)
dados <- rnorm(11500,
mean = 62,
sd = 7)
library(modeest)
mfv(dados) # moda
library(modeest)
mfv(dados) # moda
library(modeest)
mfv(dados) # moda
library(modeest)
mfv(dados) # moda
library(modeest)
mfv(dados) # moda
var(dados) # variância dos dados
sd(dados) # desvio padrão
summary(dados) # resumo dos dados
library(modeest)
mfv(dados) # moda
var(dados) # variância dos dados
sd(dados) # desvio padrão
var(dados) # variância dos dados
sd(dados) # desvio padrão
summary(dados) # resumo dos dados
mean(dados) # média
media <- median(dados) # mediana
media
media <- mean(dados) # média
media
median(dados) # mediana
media <- mean(dados) # média
media
median(dados) # mediana
var(dados) # variância dos dados
desvioPadrao <- sd(dados) # desvio padrão
desvioPadrao
coeficienteVariacao <- desvioPadrao / media * 100 # coeficiente de variação
coeficienteVariacao
boxplot(dados)
hist(dados)
hist(dados, xlab = "Dados", ylab = "Frequência", labels = "Histograma dos dados")
hist(dados, xlab = "Dados", ylab = "Frequência", title(main = "Histograma dos dados"))
hist(dados, xlab = "Dados", ylab = "Frequência", main = "Histograma dos dados")
t.test(dados)
t.test(dados, conf.level = 0.99) # intervalo de confiança
install.packages("pastecs")
library(pastecs)
library(pastecs)
stat.desc(dados)
library(pastecs)
stat.desc(dados)
library(readxl)
dadosEstudantes <- read_excel("dados.xlsx")
View(dadosEstudantes)
library(readxl)
dadosEstudantes <- read_excel("dados.xlsx")
dadosEstudantes
stat.desc(dadosEstudantes)
View(dadosEstudantes)
dadosEstudantes[Sexo == "F"]
attach(dadosEstudantes)
dadosEstudantes[Sexo == "F"]
toto = dadosEstudantes %>%
group_by(Sexo) %>%
summarise(media_idade = mean(Idade))
toto
library(dplyr)
toto = dadosEstudantes %>%
group_by(Sexo) %>%
summarise(media_idade = mean(Idade))
toto
?group_by
toto = dadosEstudantes %>%
?group_by(Sexo) %>%
summarise(stat.desc(x))
toto
toto = dadosEstudantes %>%
?group_by(Sexo) %>%
summarise(stats = stat.desc(x))
toto
toto = dadosEstudantes %>%
?group_by(Sexo) %>%
summarise(stats = stat.desc(x))
toto
toto = dadosEstudantes %>%
?group_by(Sexo) %>%
summarise(stats = stat.desc(x))
toto
toto = dadosEstudantes %>%
group_by(Sexo) %>%
summarise(across(where(is.numeric),
~ as.data.frame(t(stat.desc(.x, basic = TRUE, desc = TRUE, norm = TRUE))),
.names = "{.col}_{.fn}"))
toto
toto = dadosEstudantes %>%
?group_by(Sexo) %>%
summarise(estatisticas_idade = stat.desc(Idade))
toto
toto = dadosEstudantes %>%
?group_by(Sexo) %>%
summarise(media_idade = mean(Idade),
maximo_idade = max(Idade),
minimo_idade = min(Idade),
variancia_idade = var(Idade),
desvio_padrao_idade = sd(Idade))
toto
toto = dadosEstudantes %>%
?group_by(Sexo) %>%
summarise(media_idade = mean(Idade),
maximo_idade = max(Idade),
minimo_idade = min(Idade),
variancia_idade = var(Idade),
desvio_padrao_idade = sd(Idade))
toto
dados <- rnorm(11500,
mean = 62,
sd = 7)
dados <- round(dados, digits = 1)
media <- mean(dados) # média
media
median(dados) # mediana
library(modeest)
mfv(dados) # moda
summary(dados) # resumo dos dados
var(dados) # variância dos dados
desvioPadrao <- sd(dados) # desvio padrão
desvioPadrao
coeficienteVariacao <- desvioPadrao / media * 100 # coeficiente de variação
coeficienteVariacao
boxplot(dados)
hist(dados, xlab = "Dados", ylab = "Frequência", main = "Histograma dos dados")
t.test(dados, conf.level = 0.99) # intervalo de confiança
library(pastecs)
stat.desc(dados)
library(readxl)
dadosEstudantes <- read_excel("dados.xlsx")
dadosEstudantes
stat.desc(dadosEstudantes)
library(dplyr)
toto = dadosEstudantes %>%
?group_by(Sexo) %>%
summarise(media_idade = mean(Idade),
maximo_idade = max(Idade),
minimo_idade = min(Idade),
variancia_idade = var(Idade),
desvio_padrao_idade = sd(Idade))
toto
library(dplyr)
library(dplyr)
toto = dadosEstudantes %>%
?group_by(Sexo) %>%
summarise(media_idade = mean(Idade),
maximo_idade = max(Idade),
minimo_idade = min(Idade),
variancia_idade = var(Idade),
desvio_padrao_idade = sd(Idade))
toto
dados <- rnorm(11500,
mean = 62,
sd = 7)
dados <- round(dados, digits = 1)
dados <- rnorm(11500,
mean = 62,
sd = 7)
dados <- round(dados, digits = 1)
media <- mean(dados) # média
media
median(dados) # mediana
library(modeest)
mfv(dados) # moda
dados <- rnorm(11500,
mean = 62,
sd = 7)
dados <- round(dados, digits = 1)
media <- mean(dados) # média
media
median(dados) # mediana
library(modeest)
mfv(dados) # moda
summary(dados) # resumo dos dados
var(dados) # variância dos dados
desvioPadrao <- sd(dados) # desvio padrão
desvioPadrao
coeficienteVariacao <- desvioPadrao / media * 100 # coeficiente de variação
coeficienteVariacao
boxplot(dados)
hist(dados, xlab = "Dados", ylab = "Frequência", main = "Histograma dos dados")
t.test(dados, conf.level = 0.99) # intervalo de confiança
library(pastecs)
stat.desc(dados)
library(readxl)
dadosEstudantes <- read_excel("dados.xlsx")
dadosEstudantes
stat.desc(dadosEstudantes)
library(dplyr)
toto = dadosEstudantes %>%
?group_by(Sexo) %>%
summarise(media_idade = mean(Idade),
maximo_idade = max(Idade),
minimo_idade = min(Idade),
variancia_idade = var(Idade),
desvio_padrao_idade = sd(Idade))
toto
toto = dadosEstudantes %>%
?group_by(Sexo) %>%
summarise(media_idade = mean(Idade),
maximo_idade = max(Idade),
minimo_idade = min(Idade),
variancia_idade = var(Idade),
desvio_padrao_idade = sd(Idade))
toto
toto = dadosEstudantes %>%
?group_by(Sexo) %>%
summarise(media_idade = mean(Idade),
maximo_idade = max(Idade),
minimo_idade = min(Idade),
variancia_idade = var(Idade),
desvio_padrao_idade = sd(Idade))
toto
View(dadosEstudantes)
toto = dadosEstudantes %>%
group_by(Sexo) %>%
summarise(media_idade = mean(Idade),
maximo_idade = max(Idade),
minimo_idade = min(Idade),
variancia_idade = var(Idade),
desvio_padrao_idade = sd(Idade))
toto
toto = dadosEstudantes %>%
group_by(Sexo) %>%
summarise("Idade média" = mean(Idade),
"Idade máxima" = max(Idade),
"Idade mínima" = min(Idade),
"Variância da idade" = var(Idade),
"Desvio padrão da idade" = sd(Idade))
toto
toto = dadosEstudantes %>%
group_by(Sexo) %>%
summarise("Idade média" = mean(Idade),
"Idade máxima" = max(Idade),
"Idade mínima" = min(Idade),
"Variância da idade" = var(Idade),
"Desvio padrão da idade" = sd(Idade))
toto
